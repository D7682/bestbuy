let search = document.getElementById("gh-search-input")
search.value = "rtx 3080"
let searchBtn = document.getElementsByClassName("header-search-button")
if (searchBtn.length > 0) {
    for (const val of searchBtn) {
        val.click()
    }
}
